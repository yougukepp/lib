#include "GisRect.h"

GisRect::GisRect(const std::vector<GisPoint> &points)
{
#if 0
    /*TODO: 参数检查
     * 1、四个点
     * 2、邻边垂直
     * */
    int iMax = 0;
    iMax = p1.size()
    for(int i=0; i<iMax; i++)
    {
        m_points.push_back(points[i])
    }
#endif
}

GisRect::draw(GisColor c)
{
    //GisPolygon::draw(c);
}

#endif

